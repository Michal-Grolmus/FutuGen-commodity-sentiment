services:
  monitor:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./audio_samples:/app/audio_samples
      - ./evaluation:/app/evaluation
    restart: unless-stopped
