__pycache__/
*.pyc
.env
*.egg-info/
dist/
build/
.mypy_cache/
.pytest_cache/
.ruff_cache/
# Keep generated evaluation report (actual results, not template)
!evaluation/results/evaluation_report.md
!evaluation/results/predictions.json
*.wav
*.mp3
!audio_samples/*.wav
.venv/
venv/
.coverage
.claude/
# Internal Claude Code / handoff docs — not for publication
CLAUDE.md
docs/PROJECT_STATE.md
# signals_log.jsonl is committed as seed (historical backtests)
# Runtime additions stay but aren't re-committed; gitignore selectively:
# (we keep the file tracked but accept local modifications)
audio_samples/historical/*.webm
